apply plugin: 'com.android.library'
apply plugin: 'com.github.dcendents.android-maven'

group = 'com.github.acntwww'

android {
    compileSdkVersion 25
    buildToolsVersion "25.0.2"

    defaultConfig {
        targetSdkVersion 23
        versionCode 1
        versionName "1.0"
    }

    lintOptions {
        abortOnError false
    }
   
}